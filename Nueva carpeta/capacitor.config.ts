import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duoc.registrapp',
  appName: 'RegistrApp',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
